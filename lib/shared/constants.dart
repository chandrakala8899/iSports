enum ProfileType { individual, teamAccount, eventOrganizer }

enum EventsType {
  tournaments,
  leagues,
  charityMatches,
  multiSports,
  friendlyMatches,
  others
}



enum SportType {
  cricket,
  football,
  badminton,
  kabaddi,
  hockey,
  basketball,
  chess,
  tableTennis
}

enum ProfileVisibilityType { public, private, myTeam }

enum MessageAccessType { everyone, teams, none }

enum PaymentMode { creditOrDebit, upi }

enum RoleType{player,coach}

enum ExperienceLevel{beginner,intermediate,advanced}

enum CoachingLevel{junior,intermediate,professional}
