import 'package:flutter/material.dart';

ThemeData themeData =
    ThemeData(primaryColor: primaryColor, textTheme: TextTheme());

const primaryColor = Color(0xff003572);

const secondaryColor = Color(0xff0aa4f2);
